
import Psp from './psp.jsx'

function App() {
  // const username="psp and react"
  return (

  //  <Psp/>
    
      //lets check how to hanndle variables in react
       // un comment the above variable
    <p>hello and welcome to {username}</p>

    //we cannot directly inject variable, thus use {var_name}
    //it is called evaluated expression
    //it only sends the evaluated result and no operation should be done in it.
    //only final outcome
    //why?   go back to the main file to know
  )
}

export default App
